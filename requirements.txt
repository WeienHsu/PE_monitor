yfinance>=0.2.36
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.0.0
numpy>=1.26.0
python-dotenv>=1.0.0
finnhub-python>=2.4.19
nltk>=3.8.1
feedparser>=6.0.0
pandas-ta>=0.3.14b
# Loughran-McDonald financial sentiment dictionary (blended with VADER)
pysentiment2>=0.1.1
# Optional: FinBERT engine (SENTIMENT_ENGINE=finbert). Heavy dependency
# (torch + transformers + ~400 MB model download on first run); leave commented
# unless actively used.
# transformers>=4.40.0
# torch>=2.1.0
